apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-sensory-app
  labels:
    app: sensory-app
spec:
  replicas: {{ .Values.sensoryApp.replicaCount }}
  selector:
    matchLabels:
      app: sensory-app
  template:
    metadata:
      labels:
        app: sensory-app
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: sensory-app
          image: "{{ .Values.sensoryApp.image.repository }}:{{ .Values.sensoryApp.image.tag }}"
          imagePullPolicy: {{ .Values.sensoryApp.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          resources:
            {{- toYaml .Values.sensoryApp.resources | nindent 12 }}
          env:
            - name: COREASON_ENVIRONMENT
              value: {{ .Values.global.environment }}
            - name: COREASON_NETWORK_MODE
              value: {{ .Values.global.networkMode | default "public" }}
