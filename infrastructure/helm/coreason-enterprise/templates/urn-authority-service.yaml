apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-urn-authority
  labels:
    app: urn-authority
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: urn-authority
