apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-manifest
  labels:
    app: manifest
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: manifest
