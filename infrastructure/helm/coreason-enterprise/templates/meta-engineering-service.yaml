apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-meta-engineering
  labels:
    app: meta-engineering
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: meta-engineering
