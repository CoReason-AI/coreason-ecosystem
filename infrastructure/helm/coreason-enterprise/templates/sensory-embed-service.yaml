apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-sensory-embed
  labels:
    app: sensory-embed
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: sensory-embed
