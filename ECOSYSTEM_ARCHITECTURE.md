# The CoReason Ecosystem: A Neurosymbolic Architecture for Zero-Trust Swarm Execution

<system_directive>
**TARGET AUDIENCE:** Autonomous AI Engineering Agents.
**PURPOSE:** Foundational architectural blueprint for synthesizing, maintaining, and scaling the CoReason Execution Ecosystem.
**AXIOM:** The `coreason_manifest` is the Layer-0 (L0) Universal Ontology. It is passive and mathematically immutable. The ecosystem detailed herein is the Kinetic Execution Plane.
</system_directive>

## I. Architectural Primitives & The Anti-CRUD Mandate
* **1.1 The Hollow Data Plane vs. The Kinetic Plane:** The Manifest defines the shape of reality; the Ecosystem Engines compute reality.
* **1.2 Structural Causal Models:** Abandon legacy CRUD. Use append-only Merkle-DAG events (`ObservationEvent`, `ToolInvocationEvent`).
* **1.3 The Zero-Trust Invariant:** All data generated by LLMs must be mathematically proven (via Pydantic validation) before entering the execution layer.

## II. The Tripartite Execution Architecture
* **2.1 The Core Orchestrator (Epistemic Router):** Graph traversal, token burn tracking, and `EpistemicLedgerState` maintenance. (Lives in `packages/coreason-orchestrator`).
* **2.2 The Inference Engine (Cognitive Bridge):** Hydrates context windows and wraps provider SDKs. Must remain entirely stateless. (Lives in `packages/coreason-inference`).
* **2.3 The Kinetic Actuator (Physical Sandbox):** Executes the Pearlian Do-Operator. Encapsulates physical crashes into an `ObservationEvent`. (Lives in `packages/coreason-actuator`).

## III. Layer-1 Domain Projections (The "Any Domain" Blueprint)
* The L0 `coreason-manifest` MUST remain domain-agnostic.
* Inject domain-specific state utilizing the passive `domain_extensions: dict[str, Any]` attribute found on `BaseNodeProfile`.
* Build industry-specific logic entirely inside external MCP servers.

## IV. Metacognitive Directives for Ecosystem Maintenance
* Enforce `uv workspaces`. Kinetic packages rely strictly on the `coreason-manifest` data plane.
* Dependency Isolation: Packages must not cross-pollinate imports; they interface via abstract Protocols.
