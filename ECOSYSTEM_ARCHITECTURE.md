# <system_directive>
**TARGET AUDIENCE:** Autonomous AI Engineering Agents.
**PURPOSE:** To serve as the foundational architectural blueprint for synthesizing, maintaining, and scaling the CoReason Execution Ecosystem.
**AXIOM:** The `coreason_manifest` is the Layer-0 (L0) Universal Ontology. It is passive and mathematically immutable. The execution ecosystem detailed herein is the Kinetic Execution Plane.
# </system_directive>

## I. Architectural Primitives & The Anti-CRUD Mandate
* **1.1 The Hollow Data Plane vs. The Kinetic Plane:** Defines the absolute separation of concerns. The Manifest defines the *shape* of reality; the Ecosystem Engines *compute* reality.
* **1.2 Structural Causal Models over Tabular Logic:** Instructions for the AI to abandon Create, Read, Update, Delete (CRUD) paradigms. Employs append-only Merkle-DAG events (`ObservationEvent`, `ToolInvocationEvent`, `DefeasibleCascadeEvent`).
* **1.3 The Zero-Trust Invariant:** All data generated by LLMs must be mathematically proven (via Pydantic validation) before entering the execution layer.

## II. The Tripartite Execution Architecture
The CoReason execution runtime is split into three strictly pip-installable packages:
* **2.1 The Core Orchestrator (`coreason-orchestrator`):** The Epistemic Router. Handles graph traversal, token burn tracking, and `EpistemicLedgerState` maintenance.
* **2.2 The Inference Engine (`coreason-inference`):** The Cognitive Bridge. Hydrates context windows, projects `ActionSpaceManifest` schemas, and wraps provider SDKs. Completely stateless.
* **2.3 The Kinetic Actuator (`coreason-actuator`):** The Physical Sandbox. Executes the Pearlian Do-Operator (Python functions, MCP requests, UI interaction).

## III. Layer-1 Domain Projections (The "Any Domain" Blueprint)
*CRITICAL DIRECTIVE: The Layer-0 (L0) `coreason-manifest` MUST remain domain-agnostic. This section dictates how AI agents build domain-specific applications natively on top of the L0 kernel without polluting it.*
* **3.1 Untyped Extension Anchors (`domain_extensions`):** Inject domain-specific state utilizing the passive `domain_extensions: dict[str, Any]` attribute.
* **3.2 Domain-Specific MCP Servers:** Build industry-specific logic entirely inside external MCP servers. A Healthcare swarm uses the standard Orchestrator, but connects to a `healthcare-mcp` server projecting an `Epic_FHIR_Query` tool.
* **3.3 Strict Information Flow Control:** Map rigorous domain compliance (HIPAA, SOC2) using `InformationClassificationProfile.RESTRICTED` combined with a `SemanticFirewallPolicy`.
