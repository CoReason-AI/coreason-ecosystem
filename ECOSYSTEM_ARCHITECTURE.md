# <system_directive>
**TARGET AUDIENCE:** Autonomous AI Engineering Agents.
**PURPOSE:** To serve as the foundational architectural blueprint for synthesizing, maintaining, and scaling the CoReason Execution Ecosystem.
**AXIOM:** The `coreason_manifest` package represents the Layer-0 (L0) Universal Ontology. It is entirely passive, stateless, and mathematically immutable. The execution ecosystem detailed herein (`coreason-ecosystem`) is the **Kinetic Execution Plane**. 
# </system_directive>

## I. Architectural Primitives & The Anti-CRUD Mandate

The CoReason ecosystem explicitly abandons legacy web-application architectures (REST APIs, relational state mutation) in favor of structural causal modeling and neurosymbolic zero-trust loops.

* **1.1 The Hollow Data Plane vs. The Kinetic Plane:** The L0 Ontology (`coreason-manifest`) defines the mathematical *shape* of reality. It possesses no compute capabilities, network sockets, or execution loops. The Kinetic Execution Plane (`coreason-ecosystem`) *computes* reality by animating the ontology. Code residing in the execution plane must never redefine ontological schemas.
* **1.2 Structural Causal Models over Tabular Logic:** Agents must abandon Create, Read, Update, Delete (CRUD) paradigms. The ecosystem operates exclusively on append-only Merkle-DAG ledgers (`EpistemicLedgerState`). State is never "updated"; it is transitioned via `StateMutationIntent`s and recorded as historical `ObservationEvent`s or `BeliefMutationEvent`s.
* **1.3 The Zero-Trust Invariant:** Large Language Models (LLMs) are inherently probabilistic and prone to structural hallucinations. Therefore, all data generated by an LLM MUST be treated as mathematically hostile until proven otherwise. No LLM output may trigger a side-effect or enter the `EpistemicLedgerState` without successfully passing through a native `pydantic` validation funnel mapped to the L0 ontology.

---

## II. The Tripartite Execution Architecture

The CoReason execution runtime (`coreason-ecosystem`) operates as an **Integration Wrapper**. It is strictly forbidden from containing the monolithic source code of its subsystems. Instead, it securely routes data between three strictly decoupled, pip-installable execution engines:

### 2.1 The Core Orchestrator (`coreason-orchestrator`)
**The Epistemic Router.** This engine is responsible for maintaining macroeconomic governance over the swarm.
* **Inputs:** `WorkflowManifest`, `AnyTopologyManifest`.
* **Role:** Navigates the execution graph, manages agent handoffs, enforces `ExecutionSLA` constraints, tracks token burn footprints, and writes cryptographic checkpoints to the `EpistemicLedgerState`.
* **Constraint:** The Orchestrator is mathematically barred from prompting an LLM or executing a tool. It solely routes intents.

### 2.2 The Inference Engine (`coreason-inference`)
**The Cognitive Bridge.** The only subsystem authorized to communicate with non-deterministic neural networks.
* **Inputs:** `EpistemicLedgerState`, `AgentNodeProfile`.
* **Role:** Hydrates the immutable historical DAG into a probabilistic LLM context window. Dynamically projects strict JSON schemas derived from the active `ActionSpaceManifest`. Wraps standard provider SDKs (OpenAI, Anthropic, vLLM).
* **Constraint:** Must remain absolutely stateless. It possesses no database bindings and cannot retain cross-turn memory.

### 2.3 The Kinetic Actuator (`coreason-actuator`)
**The Physical Sandbox.** The manifestation of the Pearlian Do-Operator. The only subsystem authorized to execute physical side-effects.
* **Inputs:** `ToolInvocationEvent`, `EphemeralNamespacePartitionState`.
* **Role:** Executes native Python functions, Model Context Protocol (MCP) requests, or headless browser automation (`SpatialKinematicActionIntent`).
* **Constraint (The Epistemic Egress Law):** The Actuator MUST NEVER crash the upstream Orchestrator. Physical failures (e.g., HTTP 500s, `ZeroDivisionError`, timeouts) must be trapped, encapsulated inside a passive `ObservationEvent`, and returned to the swarm for defeasible analysis.

---

## III. Cognitive Fault Tolerance & Self-Healing

The ecosystem relies on autonomous self-healing to survive the inherent instability of generative AI.

* **3.1 System 2 Remediation Loops:** When the Inference Engine catches a `pydantic.ValidationError` (e.g., the LLM hallucinates an invalid JSON key), it MUST NOT crash. It must autonomously invoke the L0 `generate_correction_prompt()` functor, append the resulting `System2RemediationIntent` to the active context window, and force the LLM to self-correct its topological geometry before returning control to the Orchestrator.
* **3.2 Defeasible Truth Maintenance:** If a hypothesis branch yields a physical dead end, the agent emits a `DefeasibleCascadeEvent`. The Orchestrator does not delete the failed history; instead, it mathematically severs the contextual branch by adding the failed `event_id`s to a `quarantined_event_ids` array, rendering them invisible to future LLM context windows while preserving the cryptographic ledger.

---

## IV. Dimensional Scaling (Vertical & Horizontal)

AI Agents assigned to scale or optimize the ecosystem must adhere to these directives:

### 4.1 Vertical Scaling (Efficiency & Accuracy)
* **Compute Spot Routing:** Implement `RoutingFrontierPolicy` arrays to dynamically route simple operations (e.g., JSON extraction) to high-speed/low-cost models, reserving reasoning models (e.g., o1, r1) for complex topological dilemmas.
* **Test-Time Compute Escalation:** Support `EscalationContract` triggers, granting the LLM temporary authorization to increase its `LatentScratchpadReceipt` budget when detecting high-entropy failures.
* **Context Window Management:** Utilize `SemanticSlicingPolicy` bounds to mathematically prune the lowest-salience historical events from the context window, preventing VRAM exhaustion or "Lost in the Middle" syndrome.

### 4.2 Horizontal Scaling (New Capabilities)
* **Topological Modularity:** Expand functionality by introducing new execution nodes in the L0 Manifest (e.g., `SMPCNodeProfile` for secure multi-party compute) and writing the corresponding execution logic in the Orchestrator.
* **Federated Capabilities (MCP):** New swarm capabilities (e.g., querying Jira, executing SQL) MUST NOT be hardcoded into the `coreason-actuator`. They must be projected dynamically via standard `MCPServerManifest` connections, keeping the core runtime minimal and infinitely extensible.

---

## V. Layer-1 Domain Projections (The "Any Domain" Blueprint)

**CRITICAL DIRECTIVE:** The L0 Ontology (`coreason-manifest`) and the L1 Execution Plane (`coreason-ecosystem`) MUST remain strictly domain-agnostic. 
When building industry-specific applications (e.g., Healthcare, Legal, Finance, Cybersecurity), AI engineers must map domain requirements onto the agnostic primitives using the following rules:

* **5.1 Untyped Extension Anchors (`domain_extensions`):** Industry-specific identifiers (e.g., `patient_fhir_id`, `case_docket_number`, `sec_cik_code`) must be safely housed within the `domain_extensions: dict[str, Any]` attribute found on core profiles. The Orchestrator ignores this dictionary mathematically, but the Inference Engine projects it into the LLM context to ground the persona.
* **5.2 Domain-Specific MCP Servers:** A "Legal Swarm" does not require a custom "Legal Actuator". Instead, developers must build an external MCP server (e.g., `westlaw-mcp-server`) and bind its `ToolManifest` to the swarm's `ActionSpaceManifest`. The standard `coreason-actuator` will execute the JSON-RPC packet blindly, keeping domain logic strictly federated.
* **5.3 Semantic Labels (`SemanticNodeState`):** Domain knowledge graphs must utilize the generalized `SemanticNodeState`. For example, a real estate deed is modeled as a generic semantic node where `label="RealEstateDeed"` and `predicate="TRANSFERRED_TO"`.
* **5.4 Strict Information Flow Control:** Rigorous domain compliance (HIPAA, SOC2, GDPR) must be mapped using `InformationClassificationProfile.RESTRICTED`. When the Orchestrator detects this classification, it MUST mathematically verify that the target Inference API endpoint complies with the active `SemanticFirewallPolicy` prior to transmission.

---

## VI. Metacognitive Directives for Maintaining this Wrapper

When an AI Agent is tasked with modifying this repository (`coreason-ecosystem`), it MUST obey the following constraints:

1. **The Wrapper Mandate:** This repository is the master CLI/DI (Dependency Injection) configuration layer. It wires the ecosystem together.
2. **No Source Duplication:** You are strictly forbidden from writing core logic for the Orchestrator, Inference, or Actuator engines here.
3. **Delegation:** If a feature request requires altering how LLMs are prompted, you must instruct the user to shift context to the `coreason-inference` repository. If a physical sandbox requires tuning, shift to `coreason-actuator`.