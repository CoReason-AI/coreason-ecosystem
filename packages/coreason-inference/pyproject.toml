[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coreason-inference"
version = "0.0.1"
description = "Inference Engine for CoReason Ecosystem"
requires-python = ">=3.14"
dependencies = [
    "coreason-manifest",
]

[tool.hatch.build.targets.wheel]
packages = ["src/coreason_inference"]
